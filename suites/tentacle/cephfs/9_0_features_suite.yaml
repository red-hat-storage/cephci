---
tests:
  ######################################################################################################################
  # Conf File : conf/squid/cephfs/tier-2_cephfs_7-node-cluster.yaml
  # Features Covered - subvolume metrics,case insensitivity subvolumegroup,toggle snapshot visibility
  # Pre-Requisites
  # Cluster Deployment
  # Configure 4 Cephfs clients
  ######################################################################################################################
  - test:
      abort-on-fail: true
      desc: "Setup phase to deploy the required pre-requisites for running the tests."
      module: install_prereq.py
      name: "setup install pre-requisistes"
  - test:
      name: cluster deployment
      desc: Execute the cluster deployment workflow.
      module: test_cephadm.py
      polarion-id:
      config:
        verify_cluster_health: true
        steps:
          - config:
              command: bootstrap
              service: cephadm
              base_cmd_args:
                verbose: true
              args:
                mon-ip: node1
                orphan-initial-daemons: true
                skip-monitoring-stack: true
          - config:
              command: add_hosts
              service: host
              args:
                attach_ip_address: true
                labels: apply-all-labels
          - config:
              command: apply
              service: mgr
              args:
                placement:
                  label: mgr
          - config:
              command: apply
              service: mon
              args:
                placement:
                  label: mon
          - config:
              command: apply
              service: osd
              args:
                all-available-devices: true
      destroy-cluster: false
      abort-on-fail: true
  - test:
      abort-on-fail: true
      config:
        command: add
        copy_admin_keyring: true
        id: client.1
        install_packages:
          - ceph-common
          - ceph-fuse
          - samba-client
          - cifs-utils
        node: node8
      desc: "Configure the Cephfs client system 1"
      destroy-cluster: false
      module: test_client.py
      name: "configure client"
  - test:
      abort-on-fail: true
      config:
        command: add
        copy_admin_keyring: true
        id: client.2
        install_packages:
          - ceph-common
          - ceph-fuse
          - samba-client
          - cifs-utils
        node: node9
      desc: "Configure the Cephfs client system 2"
      destroy-cluster: false
      module: test_client.py
      name: "configure client"
  - test:
      abort-on-fail: true
      config:
        command: add
        copy_admin_keyring: true
        id: client.3
        install_packages:
          - ceph-common
          - ceph-fuse
          - samba-client
          - cifs-utils
        node: node10
      desc: "Configure the Cephfs client system 3"
      destroy-cluster: false
      module: test_client.py
      name: "configure client"
  - test:
      abort-on-fail: true
      config:
        command: add
        copy_admin_keyring: true
        id: client.4
        install_packages:
          - ceph-common
          - ceph-fuse
          - samba-client
          - cifs-utils
        node: node11
      desc: "Configure the Cephfs client system 4"
      destroy-cluster: false
      module: test_client.py
      name: "configure client"
  ######################################################################################################################
  # Features Covered
  # Cephfs Subvolume Metrics
  ######################################################################################################################
  - test:
      name: test cephfs subvolume metrics - functional
      module: cephfs_subvol_metrics.test_subvol_metrics.py
      polarion-id: CEPH-83621832
      desc: Test fs subvolume metrics
      abort-on-fail: false

  ######################################################################################################################
  # Feature Enhancement
  # Cephfs Case insensitivity - Subvolume
  # BZ - 2355303
  ######################################################################################################################
  - test:
      name: test cephfs attributes - subvolumegroup
      module: cephfs_case_sensitivity.case_sensitivity_subvolumegroup.py
      polarion-id: CEPH-83630825
      desc: Validate the subvolumegroup using flags for case sensitivity feature
      abort-on-fail: false
  ######################################################################################################################
  # Feature: Toggle Snapshot visibility
  # Tests : Basic,functional
  ######################################################################################################################
  - test:
      name: Toggle snapshot visibility basic test
      module: snapshot_clone.test_snapshot_visibility_basic.py
      polarion-id: CEPH-83621385
      desc: Toggle snapshot visibility basic test
      abort-on-fail: false
  - test:
      name: Toggle snapshot visibility functional test
      module: snapshot_clone.test_snapshot_visibility_multi_fs_nfs_clients.py
      polarion-id: CEPH-83621386
      desc: Toggle snapshot visibility functional test
      abort-on-fail: false
