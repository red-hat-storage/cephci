tests:
   - test:
      name: install ceph pre-requisites
      module: install_prereq.py
      abort-on-fail: true

   - test:
      name: cephadm cluster bootstrap
      module: test_cephadm.py
      desc: cephadm cluster bootstrap
      destroy-cluster: False
      abort-on-fail: true